#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv)
{
        int cycles = 0;
        char filename[32];
        if(argc >= 2) cycles = atoi(argv[2]);
        sprintf(filename,""

}
~
